<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- The actual Material 3 theme is built in Compose (see ui/theme/Theme.kt).
         This XML theme is only used at activity-launch time, before Compose
         takes over. We use Material3.DayNight.NoActionBar to give a sensible
         splash background and let Compose handle the rest. -->
    <style name="Theme.SensorsCameraDemo" parent="android:Theme.Material.Light.NoActionBar" />
</resources>
